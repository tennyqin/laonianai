<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.laonianai.mapper.UserMapper">
    <select id="findByUsername" parameterType="String" resultType="com.laonianai.entity.User">
        SELECT id, username, password, nickname, create_time as createTime
        FROM user WHERE username = #{username}
    </select>

    <insert id="add" parameterType="com.laonianai.entity.User">
        INSERT INTO user (username, password, nickname)
        VALUES (#{username}, #{password}, #{nickname})
    </insert>

    <update id="updatePassword" parameterType="com.laonianai.entity.User">
        UPDATE user SET password = #{password} WHERE id = #{id}
    </update>
</mapper>